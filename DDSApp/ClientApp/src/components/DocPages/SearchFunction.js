import React from 'rect';

function SearchForDocs(props){
    if (!props.search) {
        return null;
    }

    return (
        <div>
        Hello
        </div>
    )
}

export default SearchForDocs;
